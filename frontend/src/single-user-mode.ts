/** Copy for the sidebar tooltip and the home-page discovery card. */
export const SINGLE_USER_MODE_EXPLAINER =
  "Single-user mode: no login, and anyone who can open this URL is the admin. That's the intended setup on localhost. If a reverse proxy or tunnel is exposing this site, anyone who finds it can read, write, and manage agents. To require a login, switch to credentials mode (KS_AUTH_MODE=credentials).";
